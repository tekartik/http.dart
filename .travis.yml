# Created with https://github.com/dart-lang/mono_repo
language: dart

sudo: required
addons:
  chrome: stable

before_install:
- nvm install node
- node --version

jobs:
  include:
    - stage: test
      script: ./tool/travis.sh
      dart: dev
    - stage: test
      script: ./tool/travis.sh
      dart: stable

stages:
  - test

cache:
  directories:
    - $HOME/.pub-cache