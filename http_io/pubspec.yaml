name: tekartik_http_io
description: Http io
version: 0.6.0
publish_to: none

environment:
  sdk: '>=2.12.0-259.15.beta <3.0.0'

dependencies:
  tekartik_http:
    git:
      url: git://github.com/tekartik/http.dart
      path: http
      ref: null_safety
    version: '>=0.6.0'
  tekartik_http_test:
    git:
      url: git://github.com/tekartik/http.dart
      path: http_test
      ref: null_safety
    version: '>=0.6.0'
  shelf: any

dev_dependencies:
  test: '>=1.16.0'
  dev_test: '>=0.15.0'
  process_run: '>=0.12.0'

dependency_overrides:
  tekartik_http:
    path: ../http
  tekartik_http_test:
    path: ../http_test
