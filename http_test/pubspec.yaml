name: tekartik_http_test
description: Http test
version: 0.3.0
#homepage: https://www.example.com
#author: alex <email@example.com>

environment:
  sdk: '>=2.0.0-dev.54 <3.0.0'

dependencies:
  tekartik_http:
    git:
      url: git://github.com/tekartik/http.dart
      path: http
      ref: dart2
    version: '>=0.3.0'
  build_runner: ">=0.9.0"
  build_test:
  test: '>=1.2.0'

dev_dependencies:
  dev_test: any

dependency_overrides:
  tekartik_http:
    path: ../http